import { codeReviewPageKey, overviewPageKey } from "../../../base/app/core/keys";

export const navigationRoutes = [
	{ path: "/review", name: codeReviewPageKey },
	{ path: "/overview", name: overviewPageKey },
];
